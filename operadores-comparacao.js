const numero1 = 50;
const numero2 = 60;

if(numero1 !== numero2) { 
console.log(`A condição é verdadeira`);
} else{
    console.log(`A condição é falsa`);
}

// == igual
// != Não igual (diferente)
// > maior que
// <  menor que
// >= maior ou igual 
// <= menor ou igual
// === estritament igual (tipo)
// !== não é estritamente igual